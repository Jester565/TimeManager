import { WidgetConfigDirective } from './widget-config.directive';

describe('WidgetConfigDirective', () => {
  it('should create an instance', () => {
    const directive = new WidgetConfigDirective();
    expect(directive).toBeTruthy();
  });
});
